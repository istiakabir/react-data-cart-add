const multiplication = (first, second) => {
    let total = first * second;
    return total;
}

const divided = (first, second) => {
    let total = first / second;
    return total;

}

export {multiplication, divided};